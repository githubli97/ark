spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # MySQL8之后的驱动
    url: jdbc:mysql://mysql.ark.com:3308/ark-ddd # 数据库的JDBC连接地址
    username: root # 用户名
    password: jXpullesLBc8hUdsei # 密码
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect

ark:
  service-domain:
    gateway: localhost:10000
    identify: localhost:10010
    userinfo: localhost:10020
  service-name:
    gateway: ark-gateway
    identify: ark-identify
    userinfo: ark-userinfo
  feign:
    protocol: http
